# Mock Target Repository

This tiny Python project gives the Phase 3 demo a safe repository to inspect and
test without network access, API keys, or third-party packages.

Run its tests from this directory with:

```bash
python -m unittest discover
```
